
  <!-- PRELOADER -->
  <div class="page-loader">
    <div class="loader">Loading...</div>
  </div>
  <!-- /PRELOADER -->

 <nav class="navbar navbar-custom navbar-transparent navbar-fixed-top" role="navigation">

      <div class="container">
    
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#custom-collapse">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>

          <a><%= link_to "MTN", root_path, class: "navbar-brand" %></a>

        </div>
    
        <div class="collapse navbar-collapse" id="custom-collapse">
    
          <ul class="nav navbar-nav navbar-right">
    
            <li>
              <%= link_to "Home", root_path %>
            </li>
    
            <li>
            <% if @user %>
              <%= link_to "News Map", home_map_path %>
            <% else %>
              <%= link_to "News Map", login_path%>
            <% end %>
            </li>
    
            <li>
            <% if @user %>
              <%= link_to "Today's Events", countries_path %>
            <% else %>
              <%= link_to "Today's Events", login_path%>
            <% end %>

            </li>
                  
            <li>
              <% if @user.id %>
                <%= link_to "My Favorites", user_path(@user)%>
              <% end %>

            </li>
    
            <li>
            <% if @user %>
              <%= link_to "LogOut", logout_path%>
            <% else %>
              <%= link_to "LogIn", login_path%>
            <% end %>
            </li>
    
          </ul>
        </div>
    
      </div>

  </nav>